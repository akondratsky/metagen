- project:
    name: metagen
    jobs:
      - metagen-review:

      # - release:
      #     display-name: 'MetaGen Release Pipeline'
      #     project-type: pipeline

- job-template:
    name: 'metagen-review'
    project-type: pipeline
    dsl: !include-raw-escape:
      - 'common.groovy'
      - 'review.groovy'